language: c
compiler:
  - clang
  - gcc
sudo: false
before_script: autoreconf --install
addons:
  apt:
    packages:
    - check
install:
  - ./ci/install-dependencies.sh
script: CFLAGS="-Wall -Wextra -I`pwd`/Criterion/include" LDFLAGS="-L`pwd`/Criterion/build -Wl,-R -Wl,`pwd`/Criterion/build" ./configure && make && make check
